experiment_family_id: "f05"
experiment_family_name: "entropy_sae"
backbone: "transformer"

legacy_script_key: "train_hybrid_transformer_cnn_pool"

data:
  manifest_columns_required:
    - "score"
    - "audio_path"
    - "token_loss_path"
    - "entropy_curve_path"
  sae_features:
    required: true
    npy_glob: "sae_features_{split}.npy"
    meta_glob: "sae_features_{split}_meta.pt"
  label_column: "score"

optimization:
  batch_size: 24
  num_workers: 6
  epochs: 12
  lr: 0.0001
  weight_decay: 0.01
  eval_num_workers: 0
  prefetch_factor: 1

checkpoint:
  name_template: "f05_entropy_sae_transformer_{dataset_scope}"
