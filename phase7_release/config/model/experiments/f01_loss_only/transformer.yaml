experiment_family_id: "f01"
experiment_family_name: "loss_only"
backbone: "transformer"

input_channels:
  - "loss_curve"

architecture:
  type: "transformer_encoder"
  d_model: 256
  nhead: 8
  num_layers: 4
  dim_feedforward: 1024
  dropout: 0.1
  pool: "cls"
  output_dim: 1

release_entrypoint: "train_hybrid_transformer_cnn_pool"
