experiment_family_id: "f07"
experiment_family_name: "loss_entropy_sae"
backbone: "transformer"

input_channels:
  - "loss_curve"
  - "entropy_curve"
  - "sae_feature"

sae:
  feature_dim: 1024
  source: "musicdiscovery_musicgen_small"

fusion:
  mode: "concat_tokens_then_transformer"
  per_channel_dim: 128

architecture:
  type: "transformer_encoder"
  d_model: 256
  nhead: 8
  num_layers: 4
  dim_feedforward: 1024
  dropout: 0.15
  pool: "cls"
  output_dim: 1

release_entrypoint: "train_hybrid_transformer_cnn_pool"
