# Sparse autoencoder trained on MusicGen-small activations (musicdiscovery pipeline).
# See auxiliary.sae_sparse_training in model/experiments/registry.yaml

model_name: "facebook/musicgen-small"
hook_layer: 21
input_dim: 1024
hidden_dim: 16384
l1_lambda: 0.01
